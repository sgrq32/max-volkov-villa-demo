import type{Metadata}from'next';import'./style.css';export const metadata:Metadata={title:'Casa Avela — Private Villa · Demo',description:'A fictional private villa rental concept.',openGraph:{title:'Casa Avela',description:'Quiet architecture above the Aegean — demo concept.',type:'website'},icons:{icon:'/favicon.svg'}};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
